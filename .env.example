# Required — get one at https://console.groq.com/keys
GROQ_API_KEY=

# Optional — a shared password. Requests without it get 401.
ACCESS_CODE=

# Optional — model to try first, e.g. openai/gpt-oss-120b
GROQ_MODEL=

# Optional — requests allowed per IP per 5 minutes. Defaults to 40.
RATE_LIMIT=
